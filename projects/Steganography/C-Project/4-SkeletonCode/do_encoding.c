/*
open file
  openfile already present(encodeinfo) pass without &
  if( openfile already present(encodeinfo) == e_success)
  {
      printf success
      do_encoding
  }else
  {
  print failure
  return e_failure 
  }

*/